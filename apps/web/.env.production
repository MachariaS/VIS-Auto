# Production — Heroku API. Loaded when vite build (production mode) is used.
# No /api suffix: NestJS has no global prefix; routes are /auth/login, /health, etc.
VITE_API_BASE_URL=https://vis-auto-api-production-d71b4c628d38.herokuapp.com/api
