VITE_API_BASE_URL=https://your-api-domain.example.com

# Optional: self-hosted or paid OSRM instance for road routing in the tracking map.
# Falls back to the public demo server (router.project-osrm.org) — no SLA.
# VITE_OSRM_URL=https://your-osrm-instance.example.com
